interface RegisterFormData {
    name: string;
    email: string;
    password: string;
    phone: string;
    birthDate: string;
    confirmPassword: string;
}

interface LoginFormData {
    email: string;
    password: string;
}